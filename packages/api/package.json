{
  "name": "@swipelearn/api",
  "version": "0.0.0",
  "main": "./src/server.py",
  "scripts": {
    "dev": "uvicorn src.server:app --reload --port 8000",
    "build": "echo \"Python — no build step required\"",
    "test": "python -m pytest src/tests/ -v --tb=short",
    "lint": "ruff check src/"
  },
  "dependencies": {},
  "devDependencies": {}
}
